// import {PlatformTest} from "@tsed/common";
// import {ExampleController} from "./ExampleController";
//
//
// describe("ExampleController", () => {
//   beforeEach(() => PlatformTest.create()); // Note: Use PlatformTest instead TestContext if you use @tsed/platform-express
//   afterEach(() => PlatformTest.reset());
//
//   it("should invoke controller", async () => {
//     const controller = await PlatformTest.invoke<ExampleController>(ExampleController);
//     expect(controller).to.be.instanceOf(controller);
//   });
// });
